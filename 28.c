/**
 * Build a C program to multiply two matrices (two-dimensional arrays) entered by a user. The user will enter the order of each matrix and then its elements. If the matrices cannot be multiplied due to incompatible orders, display an error message.
 */